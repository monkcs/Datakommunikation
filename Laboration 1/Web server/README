Requirements:
    make
    g++ version >= 10.0 (with support for C++20)
    Boost C++ library
        Debian based: apt-get install libboost-all-dev
        Fedora based: dnf install boost-devel


Build:
    Run 'make all'

Run:
    Port 80 (default, privileged access required): './server.out' 
    Specified port: './server.out xxxx'
